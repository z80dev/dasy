(defcontract noret
  [a :bool
   b :bool
   cap (public :uint256)]
  (defn checkA [] :uint256 :external
    (if self/a
        (return self/cap)
        (return 100)))
  (defn setCap [:uint256 x] :external
    (setv self/cap x))
(defn setA [:bool x] :external
    (setv self/a x))
  )
