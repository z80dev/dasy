(defn useCond [:uint256 x] :uint256 :external
  (cond
    (<= x 10) (return 1)
    (<= x 20) (return 2)
    True      (return 3)))

(defn absoluteValue [:uint256 x y] :uint256 [:external :pure]
  (if (>= x y)
      (return (- x y)))
  (return (- y x)))
